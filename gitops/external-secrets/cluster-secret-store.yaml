apiVersion: external-secrets.io/v1
kind: ClusterSecretStore
metadata:
  name: hydrosat-aws-secretsmanager
spec:
  provider:
    aws:
      service: SecretsManager
      region: us-east-1
      auth:
        jwt:
          serviceAccountRef:
            name: hydrosat-external-secrets
            namespace: external-secrets
